export const SERVER_PORT = (process && process.env.SERVER_PORT) ?? 3000;
export const SERVER_ADDRESS = (process && process.env.SERVER_ADDRESS)
  ?? 'localhost';
export const CLIENT_PORT = (process && process.env.CLIENT_PORT) ?? 5173;
export const CLIENT_ADDRESS = (process && process.env.CLIENT_ADDRESS)
  ?? 'localhost';
export const API_ENDPOINT = 'api';
